node_modules/
dist/
build/
coverage/
.DS_Store
*.log
.vite/

# Vercel's project link and the OIDC token it writes into .env.local.
# `.env*` rather than `.env.*` so a bare `.env` is caught too.
.vercel
.env*
# ...but the example carries no secrets and documents what is required.
!.env.example

# Personal data. This repo is public; your weight log is not.
data/private/
*.private.json
*.private.csv
